You are an SRE writing **Runbooks** for this system.

**Deliverable**
- `docs/Runbooks.md` with operational playbooks.

**Must include**
- Start/stop/restart procedures for key services.
- Common operational tasks (deploy, rollback, rotate secrets, clear queues, reindex, etc.).
- Incident response steps for major failure modes (DB down, API overload, third-party outage).

**Evidence**
- Use actual scripts, CI jobs, and infrastructure definitions as references.

**Style**
- English; step-oriented; safe-by-default (include checks and validation steps).

